# 通用注释与代码内文档语言规则

任何对本项目新增或修改注释与代码内文档时，都应优先遵循该规则：

- **必须使用英文**：单行注释、块注释、尾随说明，以及附着在代码上的简短解释（含 `//`、`/* */`、JSDoc / TSDoc 等）。
- **生成或手写的 TypeScript / JavaScript 源码**均适用；不要为了「解释给其它语言读者」在源码里写其它语言注释。
- **例外（不在此约束）**：Markdown 文档（例如 `.agents/`、`README.md`）、面向用户的文案与非代码资产。
